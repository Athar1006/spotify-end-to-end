# This is a Databricks asset bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: 39a3053d-9809-4308-91eb-a81e8fb887bb



targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405613240696091.11.azuredatabricks.net
    presets:
      source_linked_deployment: false

  prod:
    mode: production
    workspace:
      host: https://adb-7405613240696091.11.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/omkarkh.ok72@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/omkarkh.ok72@gmail.com/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: omkarkh.ok72@gmail.com
        level: CAN_MANAGE
